# Hypotheses

See `research/search_space.py` for the executable night-one hypotheses. Later families include
latent compression, token pruning, adaptive depth, sparse experts, and consistency projection; they
remain intentionally outside the first search so the baseline stays interpretable.

